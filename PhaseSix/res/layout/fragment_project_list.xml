<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/retina_wood" >

    <TextView
        android:id="@+id/showUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/addNewProjectButton"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/addNewProjectButton"
        android:layout_marginTop="36dp"
        android:gravity="center"
        android:text="@string/placeholder"
        android:textAppearance="@android:style/TextAppearance.Medium" />

    <ListView
        android:id="@+id/projectView"
        android:layout_width="match_parent"
        android:layout_height="401dp"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/showUsername"
        android:layout_marginTop="28dp"
        android:background="@color/translucent_white"
        android:padding="5dp"
        android:textColor="@color/black"
        android:textColorHint="@color/black"
        android:visibility="visible" >
    </ListView>

    <Button
        android:id="@+id/addNewProjectButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/projectView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="53dp"
        android:gravity="center"
        android:text="@string/add_new_project" />

</RelativeLayout>